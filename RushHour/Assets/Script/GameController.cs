using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GameController : MonoBehaviour
{
    public int mMove;
    public int mScore;

    public int mLevels = 10;
    public int mMinimumMove = 6;

    // Start is called before the first frame update
    void Start()
    {
        //csvÆÄÀÏ ÀĞ±â(»ùÇÃ ¾Æ¿ôÇ²)
        int Counter = 0;
        while (Counter<mLevels)
        {
            try
            {

            }
            catch
            {

            }
        }
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
